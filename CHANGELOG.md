# Changelog

Repo-level history. Keep-a-Changelog-ish: absolute `YYYY-MM-DD` dates, short SHAs once committed.

## 2026-07-16

### Added

- Initial public release: the skills-distribution mechanism (`install.sh` per-skill symlinks + self-healing `SessionStart` refresh hook), the docs-mirror skill pair (`docs-query`, `docs-update`), `skill-creator` (based on Anthropic's, Apache 2.0), the `_shared/` flat reference pool with `tools/check-shared-drift.sh`, and repo docs (`README.md`, `CLAUDE.md`).
